# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI local state
.cli/*
aws-targets.json

# Ephemeral Staging
.cache/*
*.zip
*/staging/
